channels:
  - defaults
dependencies:
  - python=3.8  # Example: specify Python version 3.8
  - matplotlib=3.6.2
  - numpy=1.23.5
  - pillow=9.3.0
  - tqdm=4.64.1
  - scipy
  - natsort
  - pandas
  - scikit-image
  - pip:
    - wandb==0.13.5
    - opencv-python
    - logguru
    - tifffile==2023.7.10
    - zarr
#PyTorch version: 2.4.0+cu121, CUDA version: 12.1, Torchvision version: 0.19.0+cu121
# pip install imutils
#if server requirement for Pytorch and Uda not known:
#>load module pytorch (on server)
#>python
#>import torch; print(f"PyTorch version: {torch.__version__}, CUDA version: {torch.version.cuda if torch.cuda.is_available() else 'CUDA not available'}")

